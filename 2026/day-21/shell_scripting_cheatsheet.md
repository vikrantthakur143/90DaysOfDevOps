#!/bin/bash

# This is single line comment

# $VAR         Variable
# '$VAR'       String

# read   # Read value from prompt


echo $0  # print source script path.
echo $1  # print first args
echo $#  # print count totle args
echo $@  # print all args
echo $?  # print last commadn exit status




# String comparisons — =, !=, -z, -n           equle to,  not equle to,  zero,   null
# Integer comparisons — -eq, -ne, -lt, -gt, -le, -ge             equle to,  not equle to,   less then,    greater then,    less then equle to,  greater then equle to
# File test operators — -f, -d, -e, -r, -w, -x, -s               file,  directory,  ,  read,  write, executable,  sockert
# if, elif, else syntax
# Logical operators — &&, ||, !
# Case statements — case ... esac


